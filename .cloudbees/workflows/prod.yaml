apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:
jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: go-repo
          artifact-id: 9777b659-0ae0-4ae1-9e92-6843421524cb
          target-environment: Automation QA